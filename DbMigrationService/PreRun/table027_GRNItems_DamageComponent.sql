-- Return challan (GRN) line: optional text describing damaged component / part (inv_grn).
-- After this runs, update stored procedures on the database (see SP/sp0024_GRNItems_damage_component_notes.sql).
 